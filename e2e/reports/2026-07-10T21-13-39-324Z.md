# E2E Run Summary — 2026-07-10T21-13-39-324Z

**Target:** https://frontend-production-1a17.up.railway.app/
**Overall:** 17 passed, 24 failed, 11 skipped (52 total) — run status: failed

## CSV coverage

### Bulk Planning

| Ref | Test | Esito |
|---|---|---|
| H-15 | plan a full week of office presence (bulk) | ✅ passed 🧪 _mocked fallback_ |
| H-16 | book a specific room for an entire week | ⏭️ skipped 🧪 _mocked fallback_ |
| H-17 | plan to be remote for two weeks consecutively | ✅ passed |
| H-18 | plan a 4 days mission next week | ✅ passed |
| H-19 | take a parental leave (6 months) | ⏱️ timedOut — Test timeout of 30000ms exceeded. |
| H-20 | cannot extend past the 30-day rolling window | ✅ passed |

<details>
<summary>H-15</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-16</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-29 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-19</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Capacity & Waiting List

| Ref | Test | Esito |
|---|---|---|
| H-40 | hitting a full office puts the next booking on the waiting list | ⏭️ skipped |
| H-40A | occupancy shows the real seat total, never total registered headcount | ✅ passed |
| H-40B | occupancy increments by 1 immediately after booking, no reload needed | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |

<details>
<summary>H-40B (capacity.spec.ts:68:25)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-20 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 2
Received: 1

  66 |     const after = await card.locator('[data-testid="daycard-occupancy"]').textContent();
  67 |     const bookedAfter = Number((after ?? '0/0').split('/')[0]);
> 68 |     expect(bookedAfter).toBe(bookedBefore + 1);
     |                         ^
  69 |
  70 |     // Reload: must still show the correct, incremented count (not "__/__").
  71 |     await page.reload();
```

</details>

### Colleague Visibility

| Ref | Test | Esito |
|---|---|---|
| H-41 | teammates are prioritised at the top of the Daily View | ⏱️ timedOut — Test timeout of 60000ms exceeded. |
| H-42 | colleagues are correctly split by status, not lumped into "in office" | ❌ failed — Error: expect(locator).toBeVisible() failed |

<details>
<summary>H-41</summary>

```
Test timeout of 60000ms exceeded.
```

</details>

<details>
<summary>H-42 (colleague-visibility.spec.ts:133:128)</summary>

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="colleague-item"]').filter({ hasText: 'Mario' }).filter({ hasText: /remote/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="colleague-item"]').filter({ hasText: 'Mario' }).filter({ hasText: /remote/i })


  131 |
  132 |     // Each must show its own correct status label somewhere in the colleague list.
> 133 |     await expect(detail.locator('[data-testid="colleague-item"]').filter({ hasText: 'Mario' }).filter({ hasText: /remote/i })).toBeVisible();
      |                                                                                                                                ^
  134 |     await expect(detail.locator('[data-testid="colleague-item"]').filter({ hasText: 'Sara' }).filter({ hasText: /leave/i })).toBeVisible();
  135 |     await expect(detail.locator('[data-testid="colleague-item"]').filter({ hasText: 'Luca' }).filter({ hasText: /mission/i })).toBeVisible();
  136 |     await expect(detail.locator('[data-testid="colleague-item"]').filter({ hasText: 'Giulia' }).filter({ hasText: /parental/i })).toBeVisible();
```

</details>

### Confirm/Check-In

| Ref | Test | Esito |
|---|---|---|
| H-21 | check-in today from the day card "Say Good Morning" button | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-22 | check-in today from the scrolling FAB | ✅ passed |
| H-23 | check-in today from inside the detailed daily view | ✅ passed |
| H-24 | confirmation for On Leave / On a Mission / Sick is automatic — no manual check-in button | ✅ passed |
| H-25 | check-in today if remote, and Undo reverts it for real | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality |
| H-25B | editing a current day after check-in shouldn't be possible | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |

<details>
<summary>H-21 (checkin.spec.ts:354:62)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/successfully checked in/i)
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/successfully checked in/i)


  352 |     await morningBtn.click();
  353 |
> 354 |     await expect(page.getByText(/successfully checked in/i)).toBeVisible({ timeout: 5000 });
      |                                                              ^
  355 |     await expect(page.getByText(/check-in failed/i)).not.toBeVisible();
  356 |   });
  357 |
```

</details>

<details>
<summary>H-25 (checkin.spec.ts:464:28)</summary>

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: false
Received: true

  462 |       await page.waitForTimeout(500);
  463 |     }
> 464 |     expect(stillConfirmed).toBe(false);
      |                            ^
  465 |   });
  466 |
  467 |   test('[H-25b] editing a current day after check-in shouldn\'t be possible', async ({ page }) => {
```

</details>

<details>
<summary>H-25B (checkin.spec.ts:481:66)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/successfully checked in/i)
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/successfully checked in/i)


  479 |       if (await morningBtn.isVisible().catch(() => false)) {
  480 |         await morningBtn.click();
> 481 |         await expect(page.getByText(/successfully checked in/i)).toBeVisible({ timeout: 5000 });
      |                                                                  ^
  482 |       }
  483 |     }
  484 |
```

</details>

### Modify/Cancel

| Ref | Test | Esito |
|---|---|---|
| H-26 | edit any future day across status combinations | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-26B | cancelling a last-minute in-office booking works on the employee account | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-27 | cancelling an in-office booking <24h shows the last-minute alert | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-28 | last-minute unbooking is saved correctly in the stats | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-29 | cancelling an office day frees the desk | ✅ passed 🧪 _mocked fallback_ |

<details>
<summary>H-26</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-30 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-26B (modify-cancel.spec.ts:168:93)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').getByText(/remote/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').getByText(/remote/i).first()


  166 |     // (employee) account — the status must genuinely end up as Remote, not stuck.
  167 |     await openDayCard(page, today);
> 168 |     await expect(page.locator('[data-testid="daily-detail"]').getByText(/remote/i).first()).toBeVisible({ timeout: 5000 });
      |                                                                                             ^
  169 |   });
  170 | });
  171 |
```

</details>

<details>
<summary>H-27 (modify-cancel.spec.ts:87:93)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').getByText(/remote/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').getByText(/remote/i).first()


  85 |
  86 |     await openDayCard(page, today);
> 87 |     await expect(page.locator('[data-testid="daily-detail"]').getByText(/remote/i).first()).toBeVisible();
     |                                                                                             ^
  88 |   });
  89 |
  90 |   test('[H-28] last-minute unbooking is saved correctly in the stats', async ({ page }) => {
```

</details>

<details>
<summary>H-28 (modify-cancel.spec.ts:109:93)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').getByText(/remote/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').getByText(/remote/i).first()


  107 |
  108 |     await openDayCard(page, today);
> 109 |     await expect(page.locator('[data-testid="daily-detail"]').getByText(/remote/i).first()).toBeVisible({ timeout: 5000 });
      |                                                                                             ^
  110 |
  111 |     const modifyCancelAuthHeaders = await getAuthHeaders(page);
  112 |     const meRes = await page.request.get(`${API_BASE}/auth/me`, { headers: modifyCancelAuthHeaders });
```

</details>

<details>
<summary>H-29</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

### Permesso

| Ref | Test | Esito |
|---|---|---|
| H-31 | request leave hours alongside a normal day (custom amount) | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |
| H-32 | request leave hours for half a day in the morning | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |
| H-33 | request leave hours for half a day in the afternoon | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |

<details>
<summary>H-31 (permesso.spec.ts:58:34)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-03 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "custom"
Received: "CUSTOM"

  56 |     const entry = await getPresenceEntry(page, date);
  57 |     expect(entry?.status).toBe('in_office');
> 58 |     expect(entry?.offTime?.type).toBe('custom');
     |                                  ^
  59 |     expect(entry?.offTime?.hours).toBe(4);
  60 |
  61 |     // And the day card must still show the In Office icon, not the "unknown status" (?) fallback.
```

</details>

<details>
<summary>H-32 (permesso.spec.ts:85:34)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "morning"
Received: "MORNING"

  83 |     const entry = await getPresenceEntry(page, date);
  84 |     expect(entry?.status).toBe('in_office');
> 85 |     expect(entry?.offTime?.type).toBe('morning');
     |                                  ^
  86 |
  87 |     const card = page.locator(`[data-testid="day-card"][data-date="${date}"]`);
  88 |     await card.scrollIntoViewIfNeeded();
```

</details>

<details>
<summary>H-33 (permesso.spec.ts:110:34)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-20 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "afternoon"
Received: "AFTERNOON"

  108 |     const entry = await getPresenceEntry(page, date);
  109 |     expect(entry?.status).toBe('in_office');
> 110 |     expect(entry?.offTime?.type).toBe('afternoon');
      |                                  ^
  111 |
  112 |     const card = page.locator(`[data-testid="day-card"][data-date="${date}"]`);
  113 |     await card.scrollIntoViewIfNeeded();
```

</details>

### Plan a Future Day

| Ref | Test | Esito |
|---|---|---|
| H-09 | future day with no status shows the "Set status" empty state | ⏭️ skipped |
| H-10 | plan a future day — In Office | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-11 | plan a future day — Remote Working | ✅ passed |
| H-12 | plan a future day — On a Mission | ✅ passed |
| H-13 | plan a future day — On Leave | ✅ passed |
| H-14 | plan a future day — On Sick Leave (extended path) does not blank the app | ⏱️ timedOut — Test timeout of 30000ms exceeded. |

<details>
<summary>H-10</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-16 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-14</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Retrofitting

| Ref | Test | Esito |
|---|---|---|
| H-34 | retrofit a past day — On a Mission | ⏭️ skipped |
| H-35 | retrofit a past day — On Leave | ⏭️ skipped |
| H-36 | retrofit a past day — On Sick Leave | ⏭️ skipped |
| H-37 | retrofit — add Permesso hours to a past day | ⏭️ skipped |
| H-38 | retrofit — non-payroll statuses (In Office / Remote) are blocked | ⏭️ skipped |
| H-39 | retrofit — visible to other users (colleague and Director/Owner) | ⏭️ skipped |

### Role-Specific Booking

| Ref | Test | Esito |
|---|---|---|
| H-44 | employee cannot override the Lab Responsible's lab booking | ⏭️ skipped |
| H-45 | Admin Member books the Admin Room | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-45B | a plain employee cannot book the Admin Room via a direct API call | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality |
| H-46 | Director books the Management Room | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-46B | Management Room is never offered to non-Director/Owner roles | ✅ passed |

<details>
<summary>H-45 (role-booking.spec.ts:43:35)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-28 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /admin room/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /admin room/i })


  41 |     const detail = page.locator('[data-testid="daily-detail"]');
  42 |     const adminRoomOption = detail.locator('[data-testid="room-option"]').filter({ hasText: /admin room/i });
> 43 |     await expect(adminRoomOption).toBeVisible({ timeout: 5000 });
     |                                   ^
  44 |     await adminRoomOption.click();
  45 |
  46 |     const res = await page.request.get(`${API_BASE}/presence?month=${date.slice(0, 7)}`, { headers: await getAuthHeaders(page) });
```

</details>

<details>
<summary>H-45B (role-booking.spec.ts:61:26)</summary>

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 403
Received: 200

  59 |     // cross-check `room` against the caller's role-permitted room types, so this is
  60 |     // expected to currently fail (200 instead of 403) — a real, documented gap.
> 61 |     expect(res.status()).toBe(403);
     |                          ^
  62 |   });
  63 |
  64 |   test('[H-46] Director books the Management Room', async ({ page }) => {
```

</details>

<details>
<summary>H-46 (role-booking.spec.ts:75:40)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-04 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /management room/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /management room/i })


  73 |     const detail = page.locator('[data-testid="daily-detail"]');
  74 |     const managementRoomOption = detail.locator('[data-testid="room-option"]').filter({ hasText: /management room/i });
> 75 |     await expect(managementRoomOption).toBeVisible({ timeout: 5000 });
     |                                        ^
  76 |     await managementRoomOption.click();
  77 |
  78 |     const res = await page.request.get(`${API_BASE}/presence?month=${date.slice(0, 7)}`, { headers: await getAuthHeaders(page) });
```

</details>

### Sick Leave (Current Day)

| Ref | Test | Esito |
|---|---|---|
| H-30 | declaring sick leave today auto-confirms and locks the day | ❌ failed — Error: expect(locator).not.toBeVisible() failed 🧪 _mocked fallback_ |

<details>
<summary>H-30 (sick-leave-today.spec.ts:56:68)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).not.toBeVisible() failed

Locator:  locator('[data-testid="daily-detail"]').locator('button:has(svg.lucide-pen)')
Expected: not visible
Received: visible
Timeout:  5000ms

Call log:
  - Expect "not toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('button:has(svg.lucide-pen)')
    14 × locator resolved to <button class="p-2 text-primary hover:bg-primary/5 rounded-full transition-colors shrink-0">…</button>
       - unexpected value "visible"


  54 |
  55 |     // (b) the day should lock from further edits — no edit affordance...
> 56 |     await expect(detail.locator('button:has(svg.lucide-pen)')).not.toBeVisible();
     |                                                                    ^
  57 |
  58 |     // ...and the backend should reject a further status change (409).
  59 |     const res = await page.request.post(`${API_BASE}/presence`, {
```

</details>

### Stats Sanity Check

| Ref | Test | Esito |
|---|---|---|
| H-43 | only confirmed statuses feed into My Stats | ⏭️ skipped |

### Teammates

| Ref | Test | Esito |
|---|---|---|
| H-01 | add 5 real teammates from scratch from the Onboarding | ✅ passed |
| H-02 | 5 teammates added via onboarding are correctly surfaced | ❌ failed — Error: expect(locator).toHaveCount(expected) failed 🧪 _mocked fallback_ |
| H-03 | add 5 real teammates from scratch from the "Profile" section | ✅ passed |
| H-04 | 5 teammates added via "Profile" are correctly surfaced | ⏱️ timedOut — Test timeout of 60000ms exceeded. 🧪 _mocked fallback_ |
| H-05 | edit teammates — replace all 5 | ✅ passed |
| H-06 | edit teammates — replaced all 5 surfacing to the app | ⏱️ timedOut — Test timeout of 60000ms exceeded. 🧪 _mocked fallback_ |
| H-07 | edit teammates — replace a couple of people | ✅ passed |
| H-08 | edit teammates — replaced subset surface correctly | ⏱️ timedOut — Test timeout of 75000ms exceeded. 🧪 _mocked fallback_ |

<details>
<summary>H-02 (teammates.spec.ts:203:81)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-24 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('[data-testid="day-card"][data-date="2026-07-24"]').locator('[data-testid="daycard-teammate-avatar"]')
Expected: 1
Received: 0
Timeout:  10000ms

Call log:
  - Expect "toHaveCount" with timeout 10000ms
  - waiting for locator('[data-testid="day-card"][data-date="2026-07-24"]').locator('[data-testid="daycard-teammate-avatar"]')
    24 × locator resolved to 0 elements
       - unexpected value "0"


  201 |     const officeCard = page.locator(`[data-testid="day-card"][data-date="${officeDate}"]`);
  202 |     await officeCard.scrollIntoViewIfNeeded();
> 203 |     await expect(officeCard.locator('[data-testid="daycard-teammate-avatar"]')).toHaveCount(1, { timeout: 10000 });
      |                                                                                 ^
  204 |
  205 |     const remoteCard = page.locator(`[data-testid="day-card"][data-date="${remoteDate}"]`);
  206 |     await remoteCard.scrollIntoViewIfNeeded();
```

</details>

<details>
<summary>H-04</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-28 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 60000ms exceeded.
```

</details>

<details>
<summary>H-06</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 60000ms exceeded.
```

</details>

<details>
<summary>H-08</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 75000ms exceeded.
```

</details>

## Altri test (regressione / smoke pre-esistenti)

| File | Pass | Fail | Skip | Totale |
|---|---|---|---|---|
