# E2E Run Summary — 2026-07-10T13-01-27-894Z

**Target:** https://frontend-production-1a17.up.railway.app/
**Overall:** 7 passed, 35 failed, 10 skipped (52 total) — run status: failed

## CSV coverage

### Bulk Planning

| Ref | Test | Esito |
|---|---|---|
| H-15 | plan a full week of office presence (bulk) | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-16 | book a specific room for an entire week | ⏭️ skipped 🧪 _mocked fallback_ |
| H-17 | plan to be remote for two weeks consecutively | ⏱️ timedOut — Test timeout of 30000ms exceeded. |
| H-18 | plan a 4 days mission next week | ⏱️ timedOut — Test timeout of 30000ms exceeded. |
| H-19 | take a parental leave (6 months) | ⏱️ timedOut — Test timeout of 30000ms exceeded. |
| H-20 | cannot extend past the 30-day rolling window | ✅ passed |

<details>
<summary>H-15</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-16</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-29 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-17</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-18</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-19</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Capacity & Waiting List

| Ref | Test | Esito |
|---|---|---|
| H-40 | hitting a full office puts the next booking on the waiting list | ❌ failed — TypeError: rooms.reduce is not a function |
| H-40A | occupancy shows the real seat total, never total registered headcount | ❌ failed — TypeError: rooms.reduce is not a function |
| H-40B | occupancy increments by 1 immediately after booking, no reload needed | ⏱️ timedOut — Test timeout of 30000ms exceeded. |

<details>
<summary>H-40 (capacity.spec.ts:26:16)</summary>

```
TypeError: rooms.reduce is not a function

  24 |   const res = await page.request.get(`${API_BASE}/rooms`);
  25 |   const rooms = (await res.json()) as Array<{ capacity: number }>;
> 26 |   return rooms.reduce((sum, r) => sum + r.capacity, 0);
     |                ^
  27 | }
  28 |
  29 | test.describe('CSV coverage — Capacity & Waiting List', () => {
```

</details>

<details>
<summary>H-40A (capacity.spec.ts:26:16)</summary>

```
TypeError: rooms.reduce is not a function

  24 |   const res = await page.request.get(`${API_BASE}/rooms`);
  25 |   const rooms = (await res.json()) as Array<{ capacity: number }>;
> 26 |   return rooms.reduce((sum, r) => sum + r.capacity, 0);
     |                ^
  27 | }
  28 |
  29 | test.describe('CSV coverage — Capacity & Waiting List', () => {
```

</details>

<details>
<summary>H-40B</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Colleague Visibility

| Ref | Test | Esito |
|---|---|---|
| H-41 | teammates are prioritised at the top of the Daily View | ⏱️ timedOut — Test timeout of 30000ms exceeded. |
| H-42 | colleagues are correctly split by status, not lumped into "in office" | ⏱️ timedOut — Test timeout of 30000ms exceeded. |

<details>
<summary>H-41</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-42</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Confirm/Check-In

| Ref | Test | Esito |
|---|---|---|
| H-21 | check-in today from the day card "Say Good Morning" button | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-22 | check-in today from the scrolling FAB | ✅ passed |
| H-23 | check-in today from inside the detailed daily view | ✅ passed |
| H-24 | confirmation for On Leave / On a Mission / Sick is automatic — no manual check-in button | ✅ passed |
| H-25 | check-in today if remote, and Undo reverts it for real | ❌ failed — TypeError: days.find is not a function |
| H-25B | editing a current day after check-in shouldn't be possible | ⏱️ timedOut — Test timeout of 30000ms exceeded. |

<details>
<summary>H-21 (checkin.spec.ts:353:62)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/successfully checked in/i)
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/successfully checked in/i)


  351 |     await morningBtn.click();
  352 |
> 353 |     await expect(page.getByText(/successfully checked in/i)).toBeVisible({ timeout: 5000 });
      |                                                              ^
  354 |     await expect(page.getByText(/check-in failed/i)).not.toBeVisible();
  355 |   });
  356 |
```

</details>

<details>
<summary>H-25 (checkin.spec.ts:458:31)</summary>

```
TypeError: days.find is not a function

  456 |       const res = await page.request.get(`${apiBase}/presence?month=${month}`);
  457 |       const days = (await res.json()) as Array<{ date: string; isConfirmed?: boolean }>;
> 458 |       const todayEntry = days.find((d) => d.date === today);
      |                               ^
  459 |       stillConfirmed = !!todayEntry?.isConfirmed;
  460 |       if (!stillConfirmed) break;
  461 |       await page.waitForTimeout(500);
```

</details>

<details>
<summary>H-25B</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Modify/Cancel

| Ref | Test | Esito |
|---|---|---|
| H-26 | edit any future day across status combinations | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-26B | cancelling a last-minute in-office booking works on the employee account | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-27 | cancelling an in-office booking <24h shows the last-minute alert | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-28 | last-minute unbooking is saved correctly in the stats | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-29 | cancelling an office day frees the desk | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |

<details>
<summary>H-26</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-30 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-26B</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-27</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-28</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-29</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

### Permesso

| Ref | Test | Esito |
|---|---|---|
| H-31 | request leave hours alongside a normal day (custom amount) | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-32 | request leave hours for half a day in the morning | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-33 | request leave hours for half a day in the afternoon | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |

<details>
<summary>H-31</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-03 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-32</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-33</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-20 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

### Plan a Future Day

| Ref | Test | Esito |
|---|---|---|
| H-09 | future day with no status shows the "Set status" empty state | ⏭️ skipped |
| H-10 | plan a future day — In Office | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-11 | plan a future day — Remote Working | ✅ passed |
| H-12 | plan a future day — On a Mission | ✅ passed |
| H-13 | plan a future day — On Leave | ✅ passed |
| H-14 | plan a future day — On Sick Leave (extended path) does not blank the app | ⏱️ timedOut — Test timeout of 30000ms exceeded. |

<details>
<summary>H-10</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-16 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-14</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Retrofitting

| Ref | Test | Esito |
|---|---|---|
| H-34 | retrofit a past day — On a Mission | ⏭️ skipped |
| H-35 | retrofit a past day — On Leave | ⏭️ skipped |
| H-36 | retrofit a past day — On Sick Leave | ⏭️ skipped |
| H-37 | retrofit — add Permesso hours to a past day | ⏭️ skipped |
| H-38 | retrofit — non-payroll statuses (In Office / Remote) are blocked | ⏭️ skipped |
| H-39 | retrofit — visible to other users (colleague and Director/Owner) | ⏭️ skipped |

### Role-Specific Booking

| Ref | Test | Esito |
|---|---|---|
| H-44 | employee cannot override the Lab Responsible's lab booking | ⏭️ skipped |
| H-45 | Admin Member books the Admin Room | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-45B | a plain employee cannot book the Admin Room via a direct API call | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality |
| H-46 | Director books the Management Room | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-46B | Management Room is never offered to non-Director/Owner roles | ❌ failed — TypeError: rooms.some is not a function |

<details>
<summary>H-45 (role-booking.spec.ts:43:35)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-28 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /admin room/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /admin room/i })


  41 |     const detail = page.locator('[data-testid="daily-detail"]');
  42 |     const adminRoomOption = detail.locator('[data-testid="room-option"]').filter({ hasText: /admin room/i });
> 43 |     await expect(adminRoomOption).toBeVisible({ timeout: 5000 });
     |                                   ^
  44 |     await adminRoomOption.click();
  45 |
  46 |     const res = await page.request.get(`${API_BASE}/presence?month=${date.slice(0, 7)}`);
```

</details>

<details>
<summary>H-45B (role-booking.spec.ts:60:26)</summary>

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 403
Received: 401

  58 |     // cross-check `room` against the caller's role-permitted room types, so this is
  59 |     // expected to currently fail (200 instead of 403) — a real, documented gap.
> 60 |     expect(res.status()).toBe(403);
     |                          ^
  61 |   });
  62 |
  63 |   test('[H-46] Director books the Management Room', async ({ page }) => {
```

</details>

<details>
<summary>H-46 (role-booking.spec.ts:74:40)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-04 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /management room/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /management room/i })


  72 |     const detail = page.locator('[data-testid="daily-detail"]');
  73 |     const managementRoomOption = detail.locator('[data-testid="room-option"]').filter({ hasText: /management room/i });
> 74 |     await expect(managementRoomOption).toBeVisible({ timeout: 5000 });
     |                                        ^
  75 |     await managementRoomOption.click();
  76 |
  77 |     const res = await page.request.get(`${API_BASE}/presence?month=${date.slice(0, 7)}`);
```

</details>

<details>
<summary>H-46B (role-booking.spec.ts:89:20)</summary>

```
TypeError: rooms.some is not a function

  87 |       const res = await page.request.get(`${API_BASE}/rooms`);
  88 |       const rooms = (await res.json()) as Array<{ name: string; type: string }>;
> 89 |       expect(rooms.some((r) => r.type === 'management')).toBe(false);
     |                    ^
  90 |       await context.close();
  91 |     }
  92 |   });
```

</details>

### Sick Leave (Current Day)

| Ref | Test | Esito |
|---|---|---|
| H-30 | declaring sick leave today auto-confirms and locks the day | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |

<details>
<summary>H-30</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-10 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

### Stats Sanity Check

| Ref | Test | Esito |
|---|---|---|
| H-43 | only confirmed statuses feed into My Stats | ⏭️ skipped |

### Teammates

| Ref | Test | Esito |
|---|---|---|
| H-01 | add 5 real teammates from scratch from the Onboarding | ❌ failed — TypeError: users.map is not a function |
| H-02 | 5 teammates added via onboarding are correctly surfaced | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-03 | add 5 real teammates from scratch from the "Profile" section | ❌ failed — Error: expect(received).toContain(expected) // indexOf |
| H-04 | 5 teammates added via "Profile" are correctly surfaced | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-05 | edit teammates — replace all 5 | ❌ failed — Error: expect(received).toContain(expected) // indexOf |
| H-06 | edit teammates — replaced all 5 surfacing to the app | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-07 | edit teammates — replace a couple of people | ❌ failed — Error: expect(received).not.toContain(expected) // indexOf |
| H-08 | edit teammates — replaced subset surface correctly | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |

<details>
<summary>H-01 (teammates.spec.ts:30:30)</summary>

```
TypeError: users.map is not a function

  28 |   const usersRes = await request.get(`${apiBase}/admin/users`, { headers: { Authorization: `Bearer ${token}` } });
  29 |   const users = (await usersRes.json()) as Array<{ id: string; name: string }>;
> 30 |   const byId = new Map(users.map((u) => [u.id, u.name]));
     |                              ^
  31 |   return me.teammates.map((id) => byId.get(id) ?? id);
  32 | }
  33 |
```

</details>

<details>
<summary>H-02</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-24 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-03 (teammates.spec.ts:180:52)</summary>

```
Error: expect(received).toContain(expected) // indexOf

Expected value: "Mario Rossi"
Received array: ["Damiano Taurino", "Giuseppe Frau", "Tommaso Vendruscolo", "Elisa Prati", "Sara Ferrari"]

  178 |     const names = await getMyTeammateNames(request, token);
  179 |     expect(names.length).toBe(5);
> 180 |     for (const n of KNOWN_TEAMMATES) expect(names).toContain(n);
      |                                                    ^
  181 |   });
  182 |
  183 |   test('[H-04] 5 teammates added via "Profile" are correctly surfaced', async ({ page, browser }) => {
```

</details>

<details>
<summary>H-04</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-28 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-05 (teammates.spec.ts:222:19)</summary>

```
Error: expect(received).toContain(expected) // indexOf

Expected value: "Giulia Bianchi"
Received array: ["Damiano Taurino", "Giuseppe Frau", "Tommaso Vendruscolo", "Elisa Prati", "Marco Conti"]

  220 |     const names = await getMyTeammateNames(request, token);
  221 |     expect(names.length).toBe(5);
> 222 |     expect(names).toContain('Giulia Bianchi');
      |                   ^
  223 |     expect(names).not.toContain('Mario Rossi');
  224 |     expect(names).not.toContain('Sara Ferrari');
  225 |     expect(names).not.toContain('Luca Esposito');
```

</details>

<details>
<summary>H-06</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-07 (teammates.spec.ts:270:23)</summary>

```
Error: expect(received).not.toContain(expected) // indexOf

Expected value: not "Mario Rossi"
Received array:     ["Damiano Taurino", "Giuseppe Frau", "Tommaso Vendruscolo", "Elisa Prati", "Mario Rossi"]

  268 |     const names = await getMyTeammateNames(request, token);
  269 |     expect(names.length).toBe(5);
> 270 |     expect(names).not.toContain('Mario Rossi');
      |                       ^
  271 |     expect(names).not.toContain('Sara Ferrari');
  272 |     expect(names).toContain('Luca Esposito');
  273 |     expect(names).toContain('Giulia Bianchi');
```

</details>

<details>
<summary>H-08</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

## Altri test (regressione / smoke pre-esistenti)

| File | Pass | Fail | Skip | Totale |
|---|---|---|---|---|
