# E2E Run Summary — 2026-07-13T09-46-05-848Z

**Target:** https://frontend-production-1a17.up.railway.app/
**Overall:** 23 passed, 18 failed, 11 skipped (52 total) — run status: failed

## CSV coverage

### Bulk Planning

| Ref | Test | Esito |
|---|---|---|
| H-15 | plan a full week of office presence (bulk) | ✅ passed 🧪 _mocked fallback_ |
| H-16 | book a specific room for an entire week | ⏭️ skipped 🧪 _mocked fallback_ |
| H-17 | plan to be remote for two weeks consecutively | ✅ passed |
| H-18 | plan a 4 days mission next week | ✅ passed |
| H-19 | take a parental leave (6 months) | ⏱️ timedOut — Test timeout of 30000ms exceeded. |
| H-20 | cannot extend past the 30-day rolling window | ✅ passed |

<details>
<summary>H-15</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-14 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-16</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-03 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-19</summary>

```
Test timeout of 30000ms exceeded.
```

</details>

### Capacity & Waiting List

| Ref | Test | Esito |
|---|---|---|
| H-40 | hitting a full office puts the next booking on the waiting list | ⏭️ skipped |
| H-40A | occupancy shows the real seat total, never total registered headcount | ✅ passed |
| H-40B | occupancy increments by 1 immediately after booking, no reload needed | ✅ passed 🧪 _mocked fallback_ |

<details>
<summary>H-40B</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-23 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

### Colleague Visibility

| Ref | Test | Esito |
|---|---|---|
| H-41 | teammates are prioritised at the top of the Daily View | ✅ passed |
| H-42 | colleagues are correctly split by status, not lumped into "in office" | ✅ passed |

### Confirm/Check-In

| Ref | Test | Esito |
|---|---|---|
| H-21 | check-in today from the day card "Say Good Morning" button | ✅ passed 🧪 _mocked fallback_ |
| H-22 | check-in today from the scrolling FAB | ✅ passed |
| H-23 | check-in today from inside the detailed daily view | ✅ passed |
| H-24 | confirmation for On Leave / On a Mission / Sick is automatic — no manual check-in button | ✅ passed |
| H-25 | check-in today if remote, and Undo reverts it for real | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality |
| H-25B | editing a current day after check-in shouldn't be possible | ✅ passed |

<details>
<summary>H-21</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-25 (checkin.spec.ts:475:28)</summary>

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: false
Received: true

  473 |       await page.waitForTimeout(500);
  474 |     }
> 475 |     expect(stillConfirmed).toBe(false);
      |                            ^
  476 |   });
  477 |
  478 |   test('[H-25b] editing a current day after check-in shouldn\'t be possible', async ({ page }) => {
```

</details>

### Modify/Cancel

| Ref | Test | Esito |
|---|---|---|
| H-26 | edit any future day across status combinations | ✅ passed 🧪 _mocked fallback_ |
| H-26B | cancelling a last-minute in-office booking works on the employee account | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-27 | cancelling an in-office booking <24h shows the last-minute alert | ⏱️ timedOut — Test timeout of 30000ms exceeded. 🧪 _mocked fallback_ |
| H-28 | last-minute unbooking is saved correctly in the stats | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-29 | cancelling an office day frees the desk | ✅ passed 🧪 _mocked fallback_ |

<details>
<summary>H-26</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-03 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

<details>
<summary>H-26B (modify-cancel.spec.ts:205:93)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').getByText(/remote/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').getByText(/remote/i).first()


  203 |     // (employee) account — the status must genuinely end up as Remote, not stuck.
  204 |     await openDayCard(page, today);
> 205 |     await expect(page.locator('[data-testid="daily-detail"]').getByText(/remote/i).first()).toBeVisible({ timeout: 5000 });
      |                                                                                             ^
  206 |   });
  207 | });
  208 |
```

</details>

<details>
<summary>H-27</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Test timeout of 30000ms exceeded.
```

</details>

<details>
<summary>H-28 (modify-cancel.spec.ts:140:93)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').getByText(/remote/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').getByText(/remote/i).first()


  138 |
  139 |     await openDayCard(page, today);
> 140 |     await expect(page.locator('[data-testid="daily-detail"]').getByText(/remote/i).first()).toBeVisible({ timeout: 5000 });
      |                                                                                             ^
  141 |
  142 |     const modifyCancelAuthHeaders = await getAuthHeaders(page);
  143 |     const meRes = await page.request.get(`${API_BASE}/auth/me`, { headers: modifyCancelAuthHeaders });
```

</details>

<details>
<summary>H-29</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-17 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

</details>

### Permesso

| Ref | Test | Esito |
|---|---|---|
| H-31 | request leave hours alongside a normal day (custom amount) | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |
| H-32 | request leave hours for half a day in the morning | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |
| H-33 | request leave hours for half a day in the afternoon | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality 🧪 _mocked fallback_ |

<details>
<summary>H-31 (permesso.spec.ts:58:34)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-05 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "custom"
Received: "CUSTOM"

  56 |     const entry = await getPresenceEntry(page, date);
  57 |     expect(entry?.status).toBe('in_office');
> 58 |     expect(entry?.offTime?.type).toBe('custom');
     |                                  ^
  59 |     expect(entry?.offTime?.hours).toBe(4);
  60 |
  61 |     // And the day card must still show the In Office icon, not the "unknown status" (?) fallback.
```

</details>

<details>
<summary>H-32 (permesso.spec.ts:85:34)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-17 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "morning"
Received: "MORNING"

  83 |     const entry = await getPresenceEntry(page, date);
  84 |     expect(entry?.status).toBe('in_office');
> 85 |     expect(entry?.offTime?.type).toBe('morning');
     |                                  ^
  86 |
  87 |     const card = page.locator(`[data-testid="day-card"][data-date="${date}"]`);
  88 |     await card.scrollIntoViewIfNeeded();
```

</details>

<details>
<summary>H-33 (permesso.spec.ts:110:34)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-23 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "afternoon"
Received: "AFTERNOON"

  108 |     const entry = await getPresenceEntry(page, date);
  109 |     expect(entry?.status).toBe('in_office');
> 110 |     expect(entry?.offTime?.type).toBe('afternoon');
      |                                  ^
  111 |
  112 |     const card = page.locator(`[data-testid="day-card"][data-date="${date}"]`);
  113 |     await card.scrollIntoViewIfNeeded();
```

</details>

### Plan a Future Day

| Ref | Test | Esito |
|---|---|---|
| H-09 | future day with no status shows the "Set status" empty state | ⏭️ skipped |
| H-10 | plan a future day — In Office | ❌ failed — Error: expect(locator).not.toBeVisible() failed 🧪 _mocked fallback_ |
| H-11 | plan a future day — Remote Working | ✅ passed |
| H-12 | plan a future day — On a Mission | ✅ passed |
| H-13 | plan a future day — On Leave | ✅ passed |
| H-14 | plan a future day — On Sick Leave (extended path) does not blank the app | ⏱️ timedOut — Test timeout of 60000ms exceeded. |

<details>
<summary>H-10 (plan-future-day.spec.ts:82:74)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-20 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).not.toBeVisible() failed

Locator:  locator('[data-testid="room-planned-badge"]')
Expected: not visible
Received: visible
Timeout:  5000ms

Call log:
  - Expect "not toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="room-planned-badge"]')
    14 × locator resolved to <span data-testid="room-planned-badge" class="text-[10px] font-bold bg-green-500/10 text-green-600 px-2 py-0.5 rounded-full uppercase tracking-wider">Planned</span>
       - unexpected value "visible"


  80 |     await goToPlanningStep(page);
  81 |     await selectStatus(page, 'IN_OFFICE', date);
> 82 |     await expect(page.locator('[data-testid="room-planned-badge"]')).not.toBeVisible();
     |                                                                          ^
  83 |   });
  84 |
  85 |   test('[H-11] plan a future day — Remote Working', async ({ page }) => {
```

</details>

<details>
<summary>H-14</summary>

```
Test timeout of 60000ms exceeded.
```

</details>

### Retrofitting

| Ref | Test | Esito |
|---|---|---|
| H-34 | retrofit a past day — On a Mission | ⏭️ skipped |
| H-35 | retrofit a past day — On Leave | ⏭️ skipped |
| H-36 | retrofit a past day — On Sick Leave | ⏭️ skipped |
| H-37 | retrofit — add Permesso hours to a past day | ⏭️ skipped |
| H-38 | retrofit — non-payroll statuses (In Office / Remote) are blocked | ⏭️ skipped |
| H-39 | retrofit — visible to other users (colleague and Director/Owner) | ⏭️ skipped |

### Role-Specific Booking

| Ref | Test | Esito |
|---|---|---|
| H-44 | employee cannot override the Lab Responsible's lab booking | ⏭️ skipped |
| H-45 | Admin Member books the Admin Room | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-45B | a plain employee cannot book the Admin Room via a direct API call | ❌ failed — Error: expect(received).toBe(expected) // Object.is equality |
| H-46 | Director books the Management Room | ❌ failed — Error: expect(locator).toBeVisible() failed 🧪 _mocked fallback_ |
| H-46B | Management Room is never offered to non-Director/Owner roles | ✅ passed |

<details>
<summary>H-45 (role-booking.spec.ts:46:35)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-31 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /admin room/i })
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /admin room/i })


  44 |     // + re-enters planning before this point — GET /rooms restarts from scratch on that
  45 |     // fresh mount, so 5s can be tight on the shared environment's variable network latency.
> 46 |     await expect(adminRoomOption).toBeVisible({ timeout: 15000 });
     |                                   ^
  47 |     await adminRoomOption.click();
  48 |
  49 |     const res = await page.request.get(`${API_BASE}/presence?month=${date.slice(0, 7)}`, { headers: await getAuthHeaders(page) });
```

</details>

<details>
<summary>H-45B (role-booking.spec.ts:64:26)</summary>

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 403
Received: 200

  62 |     // cross-check `room` against the caller's role-permitted room types, so this is
  63 |     // expected to currently fail (200 instead of 403) — a real, documented gap.
> 64 |     expect(res.status()).toBe(403);
     |                          ^
  65 |   });
  66 |
  67 |   test('[H-46] Director books the Management Room', async ({ page }) => {
```

</details>

<details>
<summary>H-46 (role-booking.spec.ts:79:40)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-08-07 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toBeVisible() failed

Locator: locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /management room/i })
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('[data-testid="room-option"]').filter({ hasText: /management room/i })


  77 |     const managementRoomOption = detail.locator('[data-testid="room-option"]').filter({ hasText: /management room/i });
  78 |     // Same reload-then-refetch timing concern as H-45 above.
> 79 |     await expect(managementRoomOption).toBeVisible({ timeout: 15000 });
     |                                        ^
  80 |     await managementRoomOption.click();
  81 |
  82 |     const res = await page.request.get(`${API_BASE}/presence?month=${date.slice(0, 7)}`, { headers: await getAuthHeaders(page) });
```

</details>

### Sick Leave (Current Day)

| Ref | Test | Esito |
|---|---|---|
| H-30 | declaring sick leave today auto-confirms and locks the day | ❌ failed — Error: expect(locator).not.toBeVisible() failed 🧪 _mocked fallback_ |

<details>
<summary>H-30 (sick-leave-today.spec.ts:65:68)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-13 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).not.toBeVisible() failed

Locator:  locator('[data-testid="daily-detail"]').locator('button:has(svg.lucide-pen)')
Expected: not visible
Received: visible
Timeout:  5000ms

Call log:
  - Expect "not toBeVisible" with timeout 5000ms
  - waiting for locator('[data-testid="daily-detail"]').locator('button:has(svg.lucide-pen)')
    14 × locator resolved to <button class="p-2 text-primary hover:bg-primary/5 rounded-full transition-colors shrink-0">…</button>
       - unexpected value "visible"


  63 |
  64 |     // (b) the day should lock from further edits — no edit affordance...
> 65 |     await expect(detail.locator('button:has(svg.lucide-pen)')).not.toBeVisible();
     |                                                                    ^
  66 |
  67 |     // ...and the backend should reject a further status change (409).
  68 |     const res = await page.request.post(`${API_BASE}/presence`, {
```

</details>

### Stats Sanity Check

| Ref | Test | Esito |
|---|---|---|
| H-43 | only confirmed statuses feed into My Stats | ⏭️ skipped |

### Teammates

| Ref | Test | Esito |
|---|---|---|
| H-01 | add 5 real teammates from scratch from the Onboarding | ✅ passed |
| H-02 | 5 teammates added via onboarding are correctly surfaced | ❌ failed — Error: expect(locator).toHaveCount(expected) failed 🧪 _mocked fallback_ |
| H-03 | add 5 real teammates from scratch from the "Profile" section | ✅ passed |
| H-04 | 5 teammates added via "Profile" are correctly surfaced | ❌ failed — Error: expect(locator).toHaveCount(expected) failed 🧪 _mocked fallback_ |
| H-05 | edit teammates — replace all 5 | ✅ passed |
| H-06 | edit teammates — replaced all 5 surfacing to the app | ❌ failed — Error: expect(locator).toHaveCount(expected) failed 🧪 _mocked fallback_ |
| H-07 | edit teammates — replace a couple of people | ✅ passed |
| H-08 | edit teammates — replaced subset surface correctly | ❌ failed — Error: expect(locator).toHaveCount(expected) failed 🧪 _mocked fallback_ |

<details>
<summary>H-02 (teammates.spec.ts:243:81)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-27 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('[data-testid="day-card"][data-date="2026-07-27"]').locator('[data-testid="daycard-teammate-avatar"]')
Expected: 1
Received: 0
Timeout:  10000ms

Call log:
  - Expect "toHaveCount" with timeout 10000ms
  - waiting for locator('[data-testid="day-card"][data-date="2026-07-27"]').locator('[data-testid="daycard-teammate-avatar"]')
    24 × locator resolved to 0 elements
       - unexpected value "0"


  241 |     const officeCard = page.locator(`[data-testid="day-card"][data-date="${officeDate}"]`);
  242 |     await officeCard.scrollIntoViewIfNeeded();
> 243 |     await expect(officeCard.locator('[data-testid="daycard-teammate-avatar"]')).toHaveCount(1, { timeout: 10000 });
      |                                                                                 ^
  244 |
  245 |     const remoteCard = page.locator(`[data-testid="day-card"][data-date="${remoteDate}"]`);
  246 |     await remoteCard.scrollIntoViewIfNeeded();
```

</details>

<details>
<summary>H-04 (teammates.spec.ts:296:81)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-31 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('[data-testid="day-card"][data-date="2026-07-31"]').locator('[data-testid="daycard-teammate-avatar"]')
Expected: 1
Received: 0
Timeout:  10000ms

Call log:
  - Expect "toHaveCount" with timeout 10000ms
  - waiting for locator('[data-testid="day-card"][data-date="2026-07-31"]').locator('[data-testid="daycard-teammate-avatar"]')
    24 × locator resolved to 0 elements
       - unexpected value "0"


  294 |     const officeCard = page.locator(`[data-testid="day-card"][data-date="${officeDate}"]`);
  295 |     await officeCard.scrollIntoViewIfNeeded();
> 296 |     await expect(officeCard.locator('[data-testid="daycard-teammate-avatar"]')).toHaveCount(1, { timeout: 10000 });
      |                                                                                 ^
  297 |
  298 |     const remoteCard = page.locator(`[data-testid="day-card"][data-date="${remoteDate}"]`);
  299 |     await remoteCard.scrollIntoViewIfNeeded();
```

</details>

<details>
<summary>H-06 (teammates.spec.ts:352:81)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-15 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('[data-testid="day-card"][data-date="2026-07-15"]').locator('[data-testid="daycard-teammate-avatar"]')
Expected: 1
Received: 0
Timeout:  10000ms

Call log:
  - Expect "toHaveCount" with timeout 10000ms
  - waiting for locator('[data-testid="day-card"][data-date="2026-07-15"]').locator('[data-testid="daycard-teammate-avatar"]')
    24 × locator resolved to 0 elements
       - unexpected value "0"


  350 |     const officeCard = page.locator(`[data-testid="day-card"][data-date="${officeDate}"]`);
  351 |     await officeCard.scrollIntoViewIfNeeded();
> 352 |     await expect(officeCard.locator('[data-testid="daycard-teammate-avatar"]')).toHaveCount(1, { timeout: 10000 });
      |                                                                                 ^
  353 |
  354 |     const remoteCard = page.locator(`[data-testid="day-card"][data-date="${remoteDate}"]`);
  355 |     await remoteCard.scrollIntoViewIfNeeded();
```

</details>

<details>
<summary>H-08 (teammates.spec.ts:412:81)</summary>

🧪 **Mocked fallback used:** Office was at capacity for 2026-07-17 on the shared dev environment — real bookings were cleared via /admin/test/free-office-capacity (a genuine, persisted booking, not a fake one) and totalCapacity was additionally patched in the UI's /presence response to work around App.tsx's synthetic-avatar-count bug so the IN_OFFICE button was actually clickable.

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('[data-testid="day-card"][data-date="2026-07-17"]').locator('[data-testid="daycard-teammate-avatar"]')
Expected: 1
Received: 0
Timeout:  10000ms

Call log:
  - Expect "toHaveCount" with timeout 10000ms
  - waiting for locator('[data-testid="day-card"][data-date="2026-07-17"]').locator('[data-testid="daycard-teammate-avatar"]')
    24 × locator resolved to 0 elements
       - unexpected value "0"


  410 |     const officeCard = page.locator(`[data-testid="day-card"][data-date="${officeDate}"]`);
  411 |     await officeCard.scrollIntoViewIfNeeded();
> 412 |     await expect(officeCard.locator('[data-testid="daycard-teammate-avatar"]')).toHaveCount(1, { timeout: 10000 });
      |                                                                                 ^
  413 |
  414 |     const remoteCard = page.locator(`[data-testid="day-card"][data-date="${remoteDate}"]`);
  415 |     await remoteCard.scrollIntoViewIfNeeded();
```

</details>

## Altri test (regressione / smoke pre-esistenti)

| File | Pass | Fail | Skip | Totale |
|---|---|---|---|---|
