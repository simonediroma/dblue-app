{
  "name": "presence-app-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:csv": "playwright test --grep \"CSV coverage\"",
    "test:basic": "playwright test --grep-invert \"CSV coverage\"",
    "test:area": "playwright test --grep",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.4.5"
  }
}
